## Unreleased

* Adds platform-specific DRM configuration support for Widevine on Android and
  FairPlay on iOS/macOS.
* Adds optional `VideoPlayerLiveOptions` for configuring live latency and
  buffering without affecting on-demand playback.
